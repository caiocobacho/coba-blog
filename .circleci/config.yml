version: 2.1
jobs:
  build:
    docker:
      - image: cimg/node:16.10
    steps:
      - checkout
      - run: npm install
      - run: npm run build
  test:
    docker:
      - image: circleci/node:14
    steps:
      - checkout
      - run: npm install
      - run: npm run test
workflows:
  build-and-test:
    jobs:
      - build
      - test:
          requires:
            - build